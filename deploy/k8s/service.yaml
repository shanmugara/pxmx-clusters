apiVersion: v1
kind: Service
metadata:
  name: pxmx-dashboard
  namespace: pxmx-dashboard
  labels:
    app.kubernetes.io/name: pxmx-dashboard
    app.kubernetes.io/component: dashboard
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: pxmx-dashboard
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
